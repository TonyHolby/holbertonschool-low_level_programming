#include "main.h"

/**
 * add - Entry point
 *
 * Description: a function that adds two integers and returns the result
 * @a: integer
 * @b: integer
 *
 * Return: add(a, b)
 */

int add(int a, int b)
{
	return (add(a, b));
}
