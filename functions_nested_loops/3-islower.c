#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 * _islower - Entry point
 *
 * Description: a function that checks for lowercase character
 * 'c' is an unsigned char
 *
 * Return: 0
 */

int _islower(int c)
{
	if (_islower(c))
		return (1);

	else
		return (0);
}
