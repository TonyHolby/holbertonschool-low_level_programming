#include "main.h"

/**
 * cap_string - Entry point
 *
 * Description: a function that capitalizes all words of a string
 * @the_string: string
 *
 * Return: the value of the string
 */

char *cap_string(char *the_string)
{
	int i, j, c, k, length_the_string = 0;

	for (c = 0; the_string[c] != '\0'; c++)
	{
		length_the_string++;
	}

	for
	return (the_string);
}
